<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.healthyme">

    <uses-permission android:name="android.permission.SEND_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ShowTemperature"></activity>
        <activity android:name=".ShowHistoryDrug" />
        <activity android:name=".ShowMedication" />
        <activity android:name=".Temperature" />
        <activity android:name=".ConfirmTakingDrugs" />
        <activity android:name=".ShowProgram" />
        <activity android:name=".HealthActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".NewProgram" />
        <activity android:name=".AlarmReceiver" />
        <activity android:name=".NewMedication" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <receiver android:name=".AlarmReceiver" />
    </application>

</manifest>